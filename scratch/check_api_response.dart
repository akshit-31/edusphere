import 'dart:convert';
import 'package:http/http.dart' as http;

void main() async {
  // Use the same API as the app:
  // from ApiConfig.serverBaseUrl: it's probably https://edusphere-erp-frontend.onrender.com/api/
  // wait, I don't know the token.
}
